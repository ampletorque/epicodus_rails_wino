<h1>Wines</h1>

<ul>
  <% @wines.each do |wine| %>
  <li><%= link_to wine.name, wine_path(wine) %></li>
  <% end %>
</ul>


<p>
  <%= link_to "Add Wine", new_wine_path(@wine), :class => "btn btn-success" %>
</p>
